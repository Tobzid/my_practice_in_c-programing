#include <stdio.h>
/**
* assigning a and b to a variable.
* then return 0, and check the output.
*/

int main(void)
{
    int a = 26, b;
    int *ptr, *ptr1;
    ptr = &a;
    ptr1 = &b;
    b = 86;
    printf("%d %d", *ptr, *ptr1);
    return 0;
}
